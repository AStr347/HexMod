cmake_minimum_required(VERSION 3.5) # Проверка версии CMake.
									# Если версия установленой программы
									# старее указаной, произайдёт аварийный выход.

project(Date)				# Название проекта

include_directories(${PROJECT_SOURCE_DIR}/include)			# Расположение заголовочных файлов



set(SOURCE_LIB Date.cpp)		# Установка переменной со списком исходников



add_library(Date STATIC ${SOURCE_LIB})# Создание статической библиотеки

target_link_libraries(Date Convert)
